<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script>

  $('#join').on('click', function(event) {
      const form = $(this).closest('form');
    event.preventDefault();
    $.ajax({method: 'put', url: '/clubs/join/<%= club._id -%>'})
      .done(()=>alert('You are in this club now.'));
      .done(() => window.location.reload());

  $('#leave').on('click', function(event) {

    event.preventDefault();
    if (confirm('Are you sure you want to leave the club?')) {
    $.ajax({method: 'put', url: '/clubs/leave/<%= club._id -%>'})
      .done(()=>alert('You leave the club.'));
      .done(() => window.location.reload());
    };














   });





</script>
