<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script>

  $('#join').on('submit', function(event) {
    const form = $(this);
    event.preventDefault();
    if (club.memberList.indexOf(user._id)>=0){
      // error: already in the club
    }else{
      club.memberList.push(user._id);
    }

  });
</script>
